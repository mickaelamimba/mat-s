
            <div class="bg-white overflow-hidden shadow rounded-lg my-3 col-span-2">
                <div class="px-4 py-5 sm:p-6">
                    <div class="grid grid-cols-1 lg:grid-cols-1 lg:gap-4 ">
                        {{ form_row(form.imeiNumber) }}
                        {{ form_row(form.marque) }}
                        {{ form_row(form.observation) }}

                        {{ form_row(form.status) }}
                        {{ form_row(form.type) }}
                    </div>
                    <div >
                        <button type="button" class="inline-flex items-center px-4 py-2 border border-Red-300 shadow-sm text-sm font-medium rounded-md text-white bg-red-600 hover:bg-red-800 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500 btn-remove">Suprimer</button>
                    </div>
                </div>

            </div>