<form method="post" action="{{ path('confiscation_delete', {'id': confiscation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ confiscation.id) }}">
    <button class="btn inline-flex items-center px-4 py-2 border border-Red-300 shadow-sm text-sm font-medium rounded-md text-white bg-red-600 hover:bg-red-800 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500">Suprimer</button>
</form>
