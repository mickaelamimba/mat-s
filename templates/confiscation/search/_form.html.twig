{% form_theme form 'form/theme.html.twig' %}
{{ form_start(form) }}
{{ form_row(form.q) }}

<div class="flex flex-wrap justify-between ">
    {{ form_row(form.type) }}
    {{ form_row(form.state) }}
    {{ form_row(form.status) }}
</div>

{{ form_row(form.imeiNumber) }}
{{ form_row(form.pvNumber) }}
{{ form_row(form.marque) }}

<div class="mt-4" >
    <button type="submit" class="inline-flex items-center px-4 py-2 border border-sky-300 shadow-sm text-sm font-medium rounded-md text-white bg-sky-600 hover:bg-sky-800 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-sky-500 " >Recherche</button>
</div>
{{ form_end(form) }}
